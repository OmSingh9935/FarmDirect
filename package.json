{
  "name": "farmdirect",
  "version": "1.0.0",
  "description": "FarmDirect - Two-sided marketplace connecting farmers and buyers with hub logistics and escrow payments",
  "private": true,
  "scripts": {
    "seed": "npm --prefix backend run prisma:push && npm --prefix backend run seed",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "author": "FarmDirect Team",
  "license": "MIT"
}
